[project]
name = "admin-vote-roblox"
version = "2.0.0"
description = "Public Admin Vote hub with local accounts and developer feedback tools"
requires-python = ">=3.12"
dependencies = [
  "Flask==3.1.2",
  "Flask-SQLAlchemy==3.1.1",
  "psycopg[binary]==3.2.9",
]

[tool.vercel]
entrypoint = "app:app"

[tool.pytest.ini_options]
testpaths = ["tests"]
